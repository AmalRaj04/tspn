__pycache__/
*.pyc
.env

data/raw/
data/processed/
data/pyg_datasets/

models/checkpoints/
models/onnx/

# Per-fold training artifacts, regenerated by every src/training/train.py
# and train_ablations.py run (same category as checkpoints -- not curated
# results like baselines.csv / all_results.csv, which ARE tracked). Covers
# both attention_fold0.npy (full TSPN) and attention_TSPN_GCN_fold0.npy
# (ablations) naming.
results/tables/attention*fold*.npy
results/tables/train_log*fold*.csv

wandb/

.ipynb_checkpoints/