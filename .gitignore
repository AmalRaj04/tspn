__pycache__/
*.pyc
.env

data/raw/
data/processed/
data/pyg_datasets/

models/checkpoints/
models/onnx/

# Per-fold training artifacts, regenerated by every src/training/train.py
# and train_ablations.py run (same category as checkpoints -- not curated
# results like baselines.csv / all_results.csv, which ARE tracked). Covers
# both attention_fold0.npy (full TSPN) and attention_TSPN_GCN_fold0.npy
# (ablations) naming.
results/tables/attention*fold*.npy
results/tables/train_log*fold*.csv

# Phase 10 analysis intermediates (predictions + per-layer attention per
# event, ~44MB total) -- regenerable from the checkpoints via
# src/analysis/collect_model_outputs.py. The curated summary CSVs it feeds
# (interpretability_attention_leontief_correlation.csv, cascade_depth.csv,
# amplifier_sectors.csv) ARE tracked, same as baselines.csv/all_results.csv.
results/analysis/

wandb/

.ipynb_checkpoints/